const Form = () => {
  return (
    <div>
      <form>
        <input type="text" name="name" placeholder="Name"></input>
        <button type="submit">send</button>
      </form>
    </div>
  );
};

export default Form;
